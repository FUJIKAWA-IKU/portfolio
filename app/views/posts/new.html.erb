<div>
  <% @post.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
</div>
<ul class="postform">
  <%= form_for(@post, url: { controller: 'posts', action: 'create' }) do |f| %>
  <li>
      <%= f.text_area :content, class: "textarea" %>
  </li>
  <%= f.submit "新規投稿", class: "newpost" %>
  <% end %>
</ul>
